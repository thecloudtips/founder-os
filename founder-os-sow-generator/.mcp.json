{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceRoot}"
      ],
      "_purpose": "Read project briefs and input documents, write generated SOW files to output directory",
      "_required": true
    },
    "notion": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-notion"
      ],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      },
      "_purpose": "Read historical SOWs for context, accept Notion page briefs as input, optionally save generated SOWs back to Notion",
      "_required": false
    }
  }
}
