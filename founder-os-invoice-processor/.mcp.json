{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceRoot}"
      ],
      "_purpose": "Read invoice files (PDF, JPG, PNG, TIFF) from local filesystem",
      "_required": true
    },
    "notion": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-notion"
      ],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      },
      "_purpose": "Store processed invoices in accounting database and create approval requests for flagged items",
      "_required": true
    }
  }
}
