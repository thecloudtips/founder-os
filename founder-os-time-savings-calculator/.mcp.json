{
  "mcpServers": {
    "notion": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-notion"],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      },
      "_purpose": "Scan completed task counts across all Founder OS plugin Notion databases, track generated savings reports in lazy-created Time Savings Calculator DB",
      "_required": true
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${workspaceRoot}"],
      "_purpose": "Read task-estimates configuration, write generated savings and ROI reports to output path",
      "_required": true
    }
  }
}
